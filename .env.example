# Copy to .env.production and fill in, then rebuild.
#
# URL of the Cloudflare Worker backing the footer's visit counter.
# See counter-worker/README.md for how to deploy it.
#
# Leave unset and the footer falls back to a per-browser localStorage count.
VITE_COUNTER_URL=
